// `cost` is missing: the error comes from deserialisation, not validation, and
// it has to stay readable all the same.
(
    buildings: [
        (id: "house", size: (1, 1), levels: 1, role: "house", service: None, required_services: []),
    ],
)
