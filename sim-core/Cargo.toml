[package]
name = "sim-core"
edition.workspace = true
version.workspace = true

# ATTENZIONE: questa lista non cresce senza discussione (D1).
# sim-core e' un crate Rust puro: niente ECS, niente Bevy, niente I/O.
[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }
slotmap = { workspace = true }
rand = { workspace = true }
rand_pcg = { workspace = true }
blake3 = { workspace = true }

# Hook di mutazione diretta dello stato, per i test di altri crate (fase 08).
# Non e' un'estensione dell'API: l'unico canale in scrittura verso il core
# restano i Command.
[features]
test-util = []

[dev-dependencies]
proptest = { workspace = true }

[lints]
workspace = true
